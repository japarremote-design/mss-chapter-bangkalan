rules_version = '2';

// Semua akses data lewat server (Firebase Admin SDK di Next.js API routes),
// jadi akses langsung dari browser ditutup total.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
